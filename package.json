{
  "name": "transfer",
  "version": "1.0.0",
  "description": "An HTTP/HTTPS proxy server for debugging use",
  "scripts": {
    "build": "babel ./src --out-dir build",
    "dev": "babel ./src --watch --out-dir build",
    "test": "tape test/**/*.js | tap-notify | tap-diff",
    "test-server": "node test/server.js"
  },
  "author": "ohmyxm",
  "license": "ISC",
  "dependencies": {
    "chalk": "^1.1.3",
    "fmt-obj": "^1.2.0",
    "koa": "^2.0.0",
    "koa-router": "^7.0.1",
    "superagent": "^3.4.1"
  },
  "devDependencies": {
    "babel-cli": "^6.22.2",
    "babel-plugin-transform-async-to-generator": "^6.22.0",
    "tape": "^4.6.3"
  }
}
